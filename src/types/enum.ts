export enum SetPos {
	First = 1,
	Second = 2,
	Third = 3,
	Fourth = 4,
	Last = -1,
}

export enum RuleOptionType {
	ByMonthDay = "bymonthday",
	BySetPos = "bysetpos",
	Interval = "interval",
}

export enum EndOptionType {
	Never = "never",
	After = "after",
	On = "on",
}
